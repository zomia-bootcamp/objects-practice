// INPUT TYPE: Object
// Instuctions: The object passed into the function has a key speed. Increase the speed by 100 and return the object;

const fasterSpaceship = (obj) => {
    
}

module.exports = fasterSpaceship;

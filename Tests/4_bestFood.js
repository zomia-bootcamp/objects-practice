// INPUT TYPE: Object
// Instuctions: Return the captain's favorite food.
//You will need to navigate the nested object down intro 'crew' then 'captain' then return the item in the 0 index from 'favorite foods' array. 

const bestFood = (obj) => {
}

module.exports = bestFood;